[project]
name = "anna-inbox-executa"
version = "2.3.4"
description = "Anna Executa mail agent."
requires-python = ">=3.10"
dependencies = [
  "pypdf>=5,<7",
]

[dependency-groups]
build = [
  "pyinstaller>=6,<7"
]

[project.scripts]
anna-inbox-executa = "anna_inbox_executa.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
  "anna_inbox_executa",
  "mail_agent",
  "executa_sdk"
]
